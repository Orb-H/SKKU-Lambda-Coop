const functions = require('firebase-functions');
const admin = require('firebase-admin');

module.exports = {
  sendToken: functions.https.onRequest((req, res) => {

  }),
  getUserToken: functions.https.onRequest((req, res) => {

  })
};
